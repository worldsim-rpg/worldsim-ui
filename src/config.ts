/* Tunable UI constants. */

/** Typewriter speed, characters per second (previz microdecision: fast). */
export const TYPEWRITER_CPS = 32

/**
 * Location change slide, total ms (previz microdecision: scene slides out
 * left, the new one slides in from the right; round 3 fade was rejected).
 */
export const LOCATION_SLIDE_MS = 300

/** Input field grows up to this many text rows (brainstorm round 1). */
export const INPUT_MAX_ROWS = 4

/** Skill growth toast lifetime, ms (brainstorm round 6). */
export const TOAST_MS = 2500

/** localStorage key for the autosave (brainstorm round 8). */
export const SAVE_KEY = 'worldsim-ui-save-v1'
