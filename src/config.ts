/* Tunable UI constants. */

/**
 * Typewriter speed, characters per second.
 * TODO(previz-v2): final speed is being decided in previz; medium default for now.
 */
export const TYPEWRITER_CPS = 45

/** Location change cross-fade, ms (brainstorm round 3: short fade, 300-400 ms). */
export const LOCATION_FADE_MS = 350

/** Input field grows up to this many text rows (brainstorm round 1). */
export const INPUT_MAX_ROWS = 4
