# Anthropic API key. The key lives ONLY here (server side), never in the
# browser bundle and never in git. Copy this file to `.env` and fill in.
ANTHROPIC_API_KEY=

# Optional: server port (Vite dev proxy expects 8787).
PORT=8787
